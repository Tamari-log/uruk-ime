# プロジェクト名

**uruk-ime**（製品名: **Uruk IME**）です。

- Gradle `rootProject.name`: `uruk-ime`
- Android `applicationId` / パッケージ: `com.uruk.ime`（Java のパッケージ名にハイフンは使えないため `.` 区切りにしています）
- GitHub リポジトリ名も `uruk-ime` を推奨します
